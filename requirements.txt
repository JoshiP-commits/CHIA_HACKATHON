# Python 3.10, 3.11 or 3.12. chialoops pins ray==2.54.0, which has no wheel
# for Python 3.13 on Windows or Apple Silicon.
# Replay mode -- no GPU, no credentials, no API key.
#   pip install -r requirements.txt
#   python -m chia_loop.loop --bypass chia_loop/configs/bypass_replay.yaml
chialoops>=1.0.1

# Synthesis-only mode additionally needs a bf16-capable GPU (compute capability
# 8.0 or higher) and the two lines below. Triton ships inside the torch wheel;
# installing it separately invites a version mismatch. numpy is not used by this
# loop, but torch emits a startup warning without it.
#   torch==2.9.1
#   numpy

# The full live loop additionally needs Vertex AI credentials and:
#   google-genai
